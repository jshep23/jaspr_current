name: jaspr_current
description: A simple yet powerful state management library for Jaspr. Keeps your component build methods clean, with ZERO dependencies on 3rd-Party packages.
version: 0.0.1
publish_to: none
repository: https://github.com/jshep23/jaspr_current

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  current_core: ^1.0.1
  jaspr: ^0.23.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
